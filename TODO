todo list for tha plat
======================

just keep it here. cuz why not.

```
 - add in the old pages
 - get that routing up
 - change those users
 - push
 - see whats good with docker (write a dockerfile)
 - see whats good with docker-compose (write a docker-compose file)
 - new branch and see if adonis js is litty too
```